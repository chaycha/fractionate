import { BasicPage } from "../components/BasicPage";
import PublishIcon from "@mui/icons-material/Publish";

export const SubmitAssetsPage = () => {
  return <BasicPage title="Submit Assets Page" icon={<PublishIcon />} />;
};
